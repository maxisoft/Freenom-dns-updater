language: python
python:
  - "2.7"
  - "3.5"
  - "nightly" # currently points to 3.6-dev
# command to install dependencies
install: "pip install -r requirements.txt"
# command to run tests
script: python freenom_dns_updater/test/freenom_test.py