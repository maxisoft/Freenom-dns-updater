language: python
python:
  - "2.7"
  - "3.5"
  - "nightly" # currently points to 3.6-dev
# command to install dependencies
install: "pip install -r requirements.txt"
# command to run tests
script: cd freenom_dns_updater && python test/freenom_test.py